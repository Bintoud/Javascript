'use strict'

function displayVariable(str) {
    var str = 'some text';
    console.log(str);
}
displayVariable(); // j'appelle ma fonction qui a pour paramètre str et qui renvoie dans la console sa valeur 
                   // qui est un string ( chaine de caratère);