'use strict'

function displayVariable(str) {
    console.log(str);
}
    
displayVariable('some text'); // j'appelle ma fonction qui a pour paramètre str et qui renvoie dans la console sa valeur 
                   // qui est un string ( chaine de caratère);