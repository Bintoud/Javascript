

    var roundup = 1.5;

    var rounded = Math.round(roundup);

    console.log(rounded);

    // 1.5 = float;
    // Math.round = retourne l'arrondi entier le plus proche;
    // rounded  contient le résultat de la méthode Math.round(), dont la variable roundUp en argument.
    // console.log() pour afficher la valeur de la variable rounded dans le terminal
